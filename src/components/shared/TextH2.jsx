export function TextH2({ className }) {
  return (
    <h2
      className={`scroll-m-20 border-b pb-2 text-3xl font-semibold tracking-tight first:mt-0 ${className}`}
    >
      The People of the Kingdom
    </h2>
  );
}
